# Deployment-safe root requirements for this repository.
#
# Recommended install sequence for environments that need LightFM 1.17:
#   python -m pip install --upgrade "pip<25"
#   pip install "setuptools<72" wheel
#   pip install -r requirements.txt
#
# This file intentionally aggregates the runtime dependencies required by:
# - pipeline/
# - betblitz-recsys-api/

-r pipeline/requirements.txt
-r betblitz-recsys-api/requirements.txt